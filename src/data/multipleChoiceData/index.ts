
import donanimGiris from './donanim_giris';
import anakart from './anakart';
import pcb from './pcb';

export const multipleChoiceData: Record<string, any[]> = {
  donanim_giris: donanimGiris,
  anakart: anakart,
  pcb: pcb,
};